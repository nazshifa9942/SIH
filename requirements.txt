# Root requirements for the Python ML services.
#
# The repository has two Python services, each with its own pinned
# requirements file — install the one you need:
#
#   pip install -r ml/freight_forecasting/requirements.txt   # freight rate forecasting API
#   pip install -r ml/optimization/requirements.txt          # voyage optimization API
#
# The files below mirror the union of both services for quick local setup.
fastapi>=0.115,<1
uvicorn[standard]>=0.34,<1
pandas>=2.1
numpy>=1.26
scikit-learn>=1.4
xgboost>=2.0
joblib>=1.3
matplotlib>=3.8
pyarrow>=14.0
ortools>=9.10,<10
pydantic>=2.0,<3
